# LOOM 项目依赖
# 注意：建议使用 pyproject.toml 进行依赖管理
# 此文件仅为兼容性提供

# 核心依赖
aiohttp>=3.9.0
aiosqlite>=0.20.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
markdown-it-py>=3.0.0
typer>=0.9.0
pyyaml>=6.0
watchdog>=3.0.0
python-dotenv>=1.0.0
backoff>=2.2.0
keyring>=24.3.0

# 开发依赖（可选）
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
mypy>=1.7.0
flake8>=6.0.0
isort>=5.12.0
pre-commit>=3.5.0

# 向量存储（可选）
chromadb>=0.4.0
sentence-transformers>=2.2.0

# API 支持（可选）
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# CLI 增强（可选）
rich>=13.0.0
prompt-toolkit>=3.0.0