apiVersion: v1
kind: Secret
metadata:
  name: loom-secrets
  namespace: loom
type: Opaque
data:
  # 使用 base64 编码的值
  # 示例：echo -n "your-api-key" | base64
  openai-api-key: "PLACEHOLDER"  # 替换为实际的 base64 编码值
  anthropic-api-key: "PLACEHOLDER"
  database-password: "PLACEHOLDER"
